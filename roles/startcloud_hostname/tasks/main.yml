---
-
  name: "Setting hostname"
  ansible.builtin.hostname:
    name: "{{ update_hostname }}"

-
  name: "Setting /etc/hosts"
  ansible.builtin.replace:
    path: "/etc/hosts"
    regexp: "{{ item.regex }}"
    replace: "{{ item.replace }}"
  with_items:
    - { regex: "(^127.0.0.1)(.*)$", replace: '127.0.0.1   localhost {{ update_hostname }}'}
    - { regex: "(^127.0.1.1)(.*)$", replace: '127.0.1.1   localhost {{ update_hostname }}'}



